export const metadata = {
  title: "Tell us what happened",
  description:
    "Something broke or got in your way? Tell JustClarify what happened, see what others have reported, and watch reports turn green as they get fixed.",
  alternates: { canonical: "/tellme" },
};

export default function TellmeLayout({ children }) {
  return children;
}
